package com.company;

import com.sun.org.apache.xpath.internal.SourceTree;

import java.util.Scanner;

public class Main {

    public static void main(String[] args) {
        // write your code here
    }

    Scanner console = new Scanner(System.in);
    int input1 = Integer.parseInt(console.nextLine());
    int input2 = Integer.parseInt(console.nextLine());

    int x = input1;
    int y = input2;

    String userinput = input1, input2;

    int userinput;
    try(
            while(true)(
    userinput - Integer.parseInt(console.nextLine());
            )
    catch (NumberFormatException)(
            System.out.println("NAN")
            continue;
            )
    return userinput;
    )
    int z = (input1 * input1 + input2 * input2);
    System.out.print.ln(int z)
}
